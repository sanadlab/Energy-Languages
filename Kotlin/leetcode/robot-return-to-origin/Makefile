# LC-energy robot-return-to-origin (Kotlin) — CLBG-shape.
# Auto-generated by scratchpad/gen_kotlin_cells.py.
#
# Kotlin reuses the shared JVM Harness (selection/Harness.java) via
# reflection; TreeNode/ListNode are the shared PUBLIC node types so a
# public Kotlin fun can name them. The arena drives correctness/perf via
# selection/run_validate.sh + run_harness.sh — this Makefile mirrors them
# for the local `make` workflow.
LANG        = Kotlin
TEST        = robot-return-to-origin
SOURCE      = solution.kt
OUTPUT      = Harness.class
ARG         =
SEL         = ../../../selection
RUN_CMD     = kotlin -cp . Harness 200 0
COMPILE_CMD = $(JAVAC) -d . $(SEL)/Harness.java $(SEL)/TreeNode.java $(SEL)/ListNode.java && $(KOTLINC) solution.kt -cp . -d .

# validate: permanent PASS via empty-stream compare. Real correctness is
# owned by the arena oracle bound to this competition (run_validate.sh).
VALIDATION_N     = $(ARG)
REFERENCE_OUTPUT = /dev/null
BINARY_OUTPUT    = 1

JAVAC   ?= javac
KOTLINC ?= kotlinc

include ../../../perfarena.mk
