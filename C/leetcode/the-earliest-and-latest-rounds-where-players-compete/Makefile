# LC-energy the-earliest-and-latest-rounds-where-players-compete (C) — CLBG-shape.
# Auto-generated by scratchpad/gen_c_cells.py.
#
# C's LeetCode calling convention (ptr+size args, malloc'd returns via
# out-params) has no reusable ABI, so correctness + perf run through the
# codegen driver in selection/harness_c.py: gcc compiles solution.c to an
# object, a generated C++ driver declares it extern "C" and marshals every
# case. This Makefile routes the local `make` targets there.
LANG        = C
TEST        = the-earliest-and-latest-rounds-where-players-compete
SOURCE      = solution.c
OUTPUT      = driver
ARG         =
SEL         = ../../../selection
RUN_CMD     = python3 $(SEL)/harness_c.py 0.2 0
COMPILE_CMD = python3 $(SEL)/harness_c.py 0.05 0 >/dev/null

# validate: permanent PASS via empty-stream compare. Real correctness is
# owned by the arena oracle (run_validate.sh -> harness_c.py validate).
VALIDATION_N     = $(ARG)
REFERENCE_OUTPUT = /dev/null
BINARY_OUTPUT    = 1

CC ?= gcc

include ../../../perfarena.mk
