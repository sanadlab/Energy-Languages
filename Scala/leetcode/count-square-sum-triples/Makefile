# LC-energy count-square-sum-triples (Scala) — CLBG-shape. Auto-generated.
# Scala runs on the JVM via a self-contained codegen driver in
# selection/harness_scala.py (scalac Main.scala + solution.scala, run via java).
LANG        = Scala
TEST        = count-square-sum-triples
SOURCE      = solution.scala
OUTPUT      = out
ARG         =
SEL         = ../../../selection
RUN_CMD     = python3 $(SEL)/harness_scala.py 0.2 0
COMPILE_CMD = python3 $(SEL)/harness_scala.py 0.05 0 >/dev/null

VALIDATION_N     = $(ARG)
REFERENCE_OUTPUT = /dev/null
BINARY_OUTPUT    = 1

include ../../../perfarena.mk
